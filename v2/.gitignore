node_modules
.next
next-env.d.ts
*.tsbuildinfo

# Static export output — rebuilt by `npm run build`, not source.
/out/

# Production build dir, kept separate from the dev server (see next.config.mjs).
/.next-build/

# Build transcript, not source.
build.log
